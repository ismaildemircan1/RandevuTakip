## 2025-04-08 - Added node_modules to .gitignore
**Learning:** For a clean PR and to avoid massive diff sizes when installing node modules, the .gitignore file must ignore the 'node_modules' folder. I also learned to debounce an input that triggers a fetch in Javascript.
**Action:** When adding dependencies or interacting with a fresh project structure, always confirm .gitignore is present and includes 'node_modules'.
