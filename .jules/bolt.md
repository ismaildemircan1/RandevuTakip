## 2024-06-05 - Avoid redundant array traversals
**Learning:** Avoid redundant array traversals by caching the result of .find() calls and performing type conversions (like parseInt) once before reusing the value.
**Action:** Always cache the result of .find() and type conversions when reusing them.
